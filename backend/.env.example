# Generate a strong secret key:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-before-deployment

# User credentials
USER_A_PASSWORD=change-me
USER_Z_PASSWORD=change-me
ADMIN_PASSWORD=change-me

# Server config (defaults work for most setups)
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
